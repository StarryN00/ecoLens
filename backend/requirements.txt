fastapi==0.104.1
starlette==0.27.0  # fastapi 0.104.1 的直接依赖；显式锁定防止 prometheus-fastapi-instrumentator 7.x 把它拉到 >=0.30.0
uvicorn[standard]==0.24.0
sqlalchemy==2.0.23
psycopg2-binary==2.9.9
asyncpg==0.29.0
redis==5.0.1
celery==5.3.4
python-multipart==0.0.6
pillow==11.3.0
python-dotenv==1.0.0
pydantic==2.5.2
pydantic-settings==2.1.0
alembic==1.12.1
aiosqlite==0.19.0
numpy==1.26.2
scikit-learn==1.3.2
geoalchemy2==0.14.2
python-docx==1.2.0  # T5 巡检报告 / T7 操作手册 Word 文档生成

# 认证/安全依赖
# python-jose 3.3.0 受 CVE-2024-33663 / CVE-2024-33664 影响（algorithm
# confusion + 大 token DoS）。3.4.0 修了 algorithm confusion，3.5.0 进一步
# 修了 DoS 路径，所以这里直接 pin 到 3.5.0（PyPI 当前最新 3.x）。
python-jose[cryptography]==3.5.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1  # passlib 1.7.4 与 bcrypt 5.x 不兼容，必须锁 4.x
email-validator==2.1.0

# AI推理依赖
ultralytics==8.0.200
torch==2.1.1
torchvision==0.16.1
opencv-python-headless==4.8.1.78

# 可观测性
prometheus-fastapi-instrumentator==6.1.0  # 7.x 要 starlette>=0.30 与 fastapi 0.104.1 冲突，锁 6.x
sentry-sdk[fastapi]==1.40.6
python-json-logger==2.0.7

# 测试依赖
pytest==7.4.3
pytest-asyncio==0.21.1
httpx==0.25.2
